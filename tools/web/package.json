{
  "name": "voidface-web",
  "version": "0.0.1",
  "private": true,
  "description": "Voidface browser demo — runs the shipped generator locally via WebGPU/WASM.",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "onnxruntime-web": "^1.19.2"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "vite": "^5.4.0"
  }
}
